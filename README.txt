## Instalación

1. **Clona el repositorio:**

   ```bash
   git clone https://github.com/gdelgadol/CodingCaseOctoberTeam3.git
   cd CodingCaseOctoberTeam3

2. **Crea un entrono virtual (opcional)**
python -m venv venv
source venv/bin/activate  # En Linux/Mac
venv\Scripts\activate  # En Windows

3.  **Instala las dependencias**

pip install -r requirements.txt

4. Instala el modelo LLM (llama 3.2 META)

accede a https://ollama.com/, descarga la version de ollama de conveniencia
una vez instalado, usando la consola escribe 'ollama run llama3.2' se descargara el modelo (2.0 GB)